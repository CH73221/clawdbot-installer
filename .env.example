# ==================== 环境变量配置文件 ====================
# 复制此文件为 .env 并填入你的实际配置
# Windows: copy .env.example .env
# Linux/Mac: cp .env.example .env

# ==================== 安全配置 ====================

# 管理员密码（必填，请设置为强密码）
# 建议使用大小写字母+数字+特殊符号，至少12位
ADMIN_PASSWORD=YourStrongPassword!2024@Secure

# IP 白名单（可选）
# 留空则允许所有 IP 访问管理后台
# 设置多个 IP 用逗号分隔，例如: 192.168.1.1,10.0.0.1
# 注意: 请使用你的实际 IP 地址，可以通过 whatismyip.com 查询
ADMIN_IP_WHITELIST=

# 管理后台随机路径（可选，用于隐藏管理入口）
# 留空则使用默认的 admin.html
# 建议设置为随机字符串，例如: a8f3c2d4
ADMIN_PATH=

# ==================== 服务器配置 ====================

# 服务端口（可选，默认 3000）
PORT=3000

# ==================== 使用说明 ====================
# 1. 复制此文件为 .env
# 2. 设置强密码
# 3. (可选) 设置 IP 白名单以提高安全性
# 4. (可选) 设置自定义管理后台路径
# 5. 使用 PM2 启动时，确保 .env 文件在同一目录
